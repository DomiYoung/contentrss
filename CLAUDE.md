# ContentRSS 项目上下文

> **自动读取**：此文件由 Claude Code 在每次会话开始时自动读取。

---

## 项目概述

ContentRSS 是一个 **Industry Intelligence OS**，面向专业人士提供 AI 驱动的行业情报服务。

| 层级 | 技术栈 |
|:---|:---|
| 前端 | React 19 + Vite 7 + TypeScript + TailwindCSS + framer-motion |
| 后端 | Flask + Python |
| 数据库 | SQLite (开发) / PostgreSQL (生产, Railway) |
| 测试 | Playwright |
| 设计 | Apple Design Scientist 风格，Paper Cream (#FAF9F6) 背景 |

---

## 核心理念

> **Superpowers + Spec-Driven + KI自进化 + 中文输出**

- **Superpowers 工作流**: 使用 `/superpowers:brainstorm` → `/superpowers:write-plan` → `/superpowers:execute-plan` 标准流程
- **Spec-First**: 所有功能规范存放在 `specs/{feature_id}/`，包含 spec.md + data-model.md + contracts/api-spec.json + plan.md
- **契约先行**: OpenAPI 规范是前后端协作的唯一真理
- **KI 自进化**: 每次会话结束前沉淀经验到 KI 系统
- **中文输出**: 所有回复、思考过程及任务清单使用中文

---

## 🏷️ 任务分类（自动执行）

**每次接收任务时，先分类再执行：**

### 类型分类
| 类型 | 特征 | 工作流 |
|:---|:---|:---|
| **Feature** | "我想做"、"新增"、"添加" | component-reuse-expert → brainstorming → spec-first → 开发 → ki-manager |
| **Bug** | "报错"、"不工作"、"修复" | 复现 → 定位 → 修复 → 验证（无需 spec） |
| **Improvement** | "优化"、"改进"、"重构" | 分析 → 方案 → 实施 → 对比 |

### 领域分类
| 领域 | 特征 | 主要 Skills |
|:---|:---|:---|
| **UI** | 样式、布局、动画 | frontend-design, framer-motion-expert |
| **Logic** | API、接口、数据处理 | backend-expert, flask-expert |
| **Data** | 数据库、表、查询 | database-expert |

---

## 🔄 强制自动化行为

### 会话启动时（自动执行）

1. **读取 KI 踩坑记录**（避免重复踩坑）
   ```
   ~/.gemini/antigravity/knowledge/contentrss/artifacts/technical/pitfalls.md
   ```

2. **读取 SESSION.md**（恢复上次进度，如存在）

### 开发新功能前（自动执行）

1. **触发 component-reuse-expert** 检查复用机会
   - 检查 `frontend/src/components/` 是否有类似组件
   - 检查 `backend/main.py` 是否有类似 API
   - 检查 `frontend/src/lib/` 是否有可复用函数

2. **生成可复用组件清单** 到 plan.md

### 功能完成后（自动执行）

1. **触发 ki-manager** 反思沉淀
   - 检查是否踩坑需记录
   - 检查是否有架构决策
   - 检查是否有组件模式

2. **更新 KI 文件**（pitfalls.md / component_patterns.md 等）

---

## 🛡️ 需求冻结保护

**Hooks 已配置**：修改 `specs/` 目录下的规范文件时会触发警告。

受保护文件：
- `specs/*/spec.md` - 需求规范
- `specs/*/data-model.md` - 数据模型
- `specs/*/contracts/*.json` - API 契约

如需修改，需用户显式确认。

---

## 常用命令

```bash
# 前端开发
cd frontend && npm run dev

# 后端开发
cd backend && uvicorn main:app --reload

# 运行测试
python scripts/with_server.py tests/v2_regression_test.py
```

---

## 文档结构

```
specs/{feature_id}/         # 功能规范（Superpowers 产出）
├── spec.md                 # 📋 需求清单（验收标准 AC-XXX）
├── data-model.md           # 数据模型
├── contracts/api-spec.json # OpenAPI 契约
├── plan.md                 # 实施计划（含可复用组件清单）
├── completion.md           # 📋 完成清单（开发实际完成情况）
└── acceptance-report.md    # 📊 验收报告（核对结果）

docs/
├── workflows/              # 工作流文档
├── v2-design/              # V2 设计文档
└── agent-evolution/        # Agent 配置诊断

~/.gemini/antigravity/knowledge/contentrss/  # KI 知识库
├── artifacts/technical/    # 技术知识（踩坑、架构、模式）
└── artifacts/product/      # 产品知识（功能决策）
```

---

## ✅ 双清单核对机制（保证功能完整性）

**核心理念**：需求清单 + 完成清单 → 逐项核对 → 验收报告

### 工作流

```
1️⃣ 需求阶段
   spec.md 包含验收标准 (AC-001, AC-002...)
       ↓
2️⃣ 开发阶段
   按 plan.md 逐项实现
       ↓
3️⃣ 完成阶段 (product-manager 自动触发)
   生成 completion.md (每个 AC 的实现状态)
       ↓
4️⃣ 验收阶段
   核对 spec.md vs completion.md
       ↓
5️⃣ 输出
   acceptance-report.md (差异说明 + 后续行动)
```

### 验收标准判定

| 条件 | 结论 |
|:---|:---|
| P0 完成率 100% + P1 ≥ 80% | ✅ **通过** |
| P0 完成率 ≥ 80% + 有后续计划 | ⚠️ **条件通过** |
| P0 完成率 < 80% | ❌ **不通过** |

---

## Skills 使用原则

本项目配置了 35+ 专业 Skills，关键 Skills：

| Skill | 触发场景 |
|:---|:---|
| **component-reuse-expert** | 开发新功能前自动检查复用 |
| **ki-manager** | 会话结束前沉淀经验 |
| **product-manager** | 进度汇报、Checklist 核对 |
| **senior-analyst** | 分析行业情报、极性判断 |
| **frontend-expert** | 前端组件开发 |
| **backend-expert** | 后端 API 开发 |

Claude Code 会根据任务自动选择合适的 Skill。**1% 原则**：如果有 1% 的可能性某个 skill 适用，必须调用它。

---

## 📅 进化日志

| 日期 | 更新内容 | 来源 |
|:---|:---|:---|
| 2025-12-28 | **双清单核对机制**: spec.md(需求) + completion.md(完成) → acceptance-report.md(验收) | 用户建议 |
| 2025-12-28 | 增加任务分类体系（Feature/Bug/Improvement + UI/Logic/Data）| 持久性保障 |
| 2025-12-28 | 增加强制自动化行为、需求冻结保护、KI集成 | 治理体系审计 |
| 2025-12-27 | 安装 Superpowers，标准化工作流，精简配置 | 工作流诊断 |
| 2025-12-27 | 移除触发器映射表，遵循 claude-code-guide 最佳实践 | 审核建议 |

---

## ⚠️ 持久性保障

**本文件 (CLAUDE.md) 由 Claude Code 在每次会话启动时自动读取。**

上述所有规则（任务分类、强制自动化行为、需求冻结保护）将在每次新会话中自动生效，无需用户重复指示。

如果发现规则未被遵循，请检查：
1. CLAUDE.md 是否在项目根目录
2. 规则是否被明确写入（而非仅引用 Skills）
3. 是否有冲突的全局配置覆盖
