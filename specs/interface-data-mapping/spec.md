# 界面-数据源映射规范

> **版本**: 1.0.0
> **更新日期**: 2025-12-29
> **状态**: 已发布

---

## 📋 概述

本文档定义 ContentRSS 各界面与后端数据源的映射关系，确保前后端契约一致性。

### 数据层级架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端界面层 (Views)                       │
├─────────────────────────────────────────────────────────────┤
│  IntelligenceView │ DataView │ ArticleDetail │ EntityRadar  │
│       (首页)       │ (数据中心) │   (详情页)    │  (实体雷达)   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      前端缓存层 (IndexedDB)                   │
│  • 基于 ingested_at 判断缓存有效性                            │
│  • 同一天内复用缓存，跨日自动失效                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端 API 层 (Flask)                      │
├─────────────────────────────────────────────────────────────┤
│  /api/intelligence │ /api/raw-data │ /api/article/<id>      │
│  /api/entities     │ /api/briefing/daily                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端缓存层 (AI分析缓存)                   │
│  • ai_polarity, ai_impacts, ai_opinion, ai_tags 字段        │
│  • 避免重复调用 AI 分析                                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据库层 (SQLite/PostgreSQL)             │
│  • raw_articles 表：原始文章数据 + AI分析缓存                  │
│  • reading_records 表：阅读记录                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      外部数据源 (Special API)                 │
│  • 飞书多维表格 → Special API → raw_articles 表               │
│  • 每日同步一次                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 界面-数据源映射表

### 1. IntelligenceView (情报首页)

| 属性 | 说明 |
|:---|:---|
| **路由** | `/` |
| **API 端点** | `GET /api/intelligence` |
| **数据类型** | AI 加工后的情报卡片 |
| **缓存策略** | IndexedDB 缓存，同日有效 |

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| `limit` | number | 否 | 返回数量，默认 20 |
| `skip_ai` | boolean | 否 | 跳过 AI 分析，默认 false |
| `category` | string | 否 | 分类筛选 (ai/digital/legal/finance/vc) |

#### 响应结构

```json
{
  "success": true,
  "data": {
    "cards": [
      {
        "id": 12345,
        "title": "文章标题",
        "polarity": "positive|negative|neutral",
        "fact": "核心事实",
        "impacts": [
          {"entity": "实体名", "trend": "up|down", "reason": "原因"}
        ],
        "opinion": "分析观点",
        "tags": ["标签1", "标签2"],
        "source_name": "来源名称",
        "source_url": "https://..."
      }
    ]
  },
  "meta": {
    "count": 20
  }
}
```

#### 前端组件

| 组件 | 文件 | 职责 |
|:---|:---|:---|
| IntelligenceView | `views/IntelligenceView.tsx` | 页面容器 |
| IntelligenceCard | `components/IntelligenceCard.tsx` | 卡片展示 |

---

### 2. DataView (数据中心)

| 属性 | 说明 |
|:---|:---|
| **路由** | `/data` |
| **API 端点** | `GET /api/raw-data` |
| **数据类型** | 原始公众号文章数据（未经 AI 处理） |
| **缓存策略** | IndexedDB 缓存，同日有效 |

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| `category` | string | 是 | 分类 key (ai/digital/legal/finance/vc) |
| `date` | string | 否 | 日期筛选，格式 YYYY-MM-DD |

#### 响应结构

```json
{
  "success": true,
  "data": {
    "category": "ai",
    "label": "AI",
    "items": [
      {
        "record_id": "recXXX",
        "ingested_at": "2025-12-29T08:39:59",
        "fields": {
          "文章信息": "{\"摘要\":\"...\",\"文章标题\":\"...\"}",
          "文章标题-moss用": "直接可用的标题",
          "raw_url": "https://..."
        }
      }
    ],
    "date_filter": null
  },
  "meta": {
    "count": 40,
    "filtered_by_date": false
  }
}
```

#### ⚠️ 数据解析注意事项

**关键字段映射**：

| 前端需要 | 后端字段 | 解析方式 |
|:---|:---|:---|
| `title` | `fields["文章标题-moss用"]` | 直接使用 |
| `title` (备选) | `fields["文章信息"].文章标题` | JSON.parse 后访问 |
| `description` | `fields["文章信息"].摘要` | JSON.parse 后访问 |
| `id` | `record_id` | 直接使用 |
| `date` | `ingested_at` | 直接使用 |

**解析示例**：

```typescript
function parseRawItem(item: Record<string, unknown>): ArticleItem {
    const fields = item.fields as Record<string, unknown> || {};

    // 优先使用直接字段
    let title = fields["文章标题-moss用"] as string;
    let description = "";

    // 解析嵌套 JSON
    const infoStr = fields["文章信息"] as string;
    if (infoStr && typeof infoStr === 'string') {
        try {
            const info = JSON.parse(infoStr);
            title = title || info.文章标题;
            description = info.摘要 || "";
        } catch (e) {
            console.warn("JSON parse failed:", e);
        }
    }

    return {
        id: item.record_id as string,
        title: title || "Untitled",
        description: description || "No description",
        date: item.ingested_at as string
    };
}
```

#### 前端组件

| 组件 | 文件 | 职责 |
|:---|:---|:---|
| DataView | `views/DataView.tsx` | 页面容器 + 分类筛选 |

---

### 3. ArticleDetail (文章详情)

| 属性 | 说明 |
|:---|:---|
| **路由** | `/article/:id` |
| **API 端点** | `GET /api/article/<id>` |
| **数据类型** | 单篇文章详情 + AI 分析 |
| **缓存策略** | 无前端缓存（按需获取） |

#### 响应结构

```json
{
  "id": 12345,
  "title": "文章标题",
  "polarity": "positive",
  "fact": "核心事实",
  "impacts": [...],
  "opinion": "分析观点",
  "tags": ["标签1"],
  "source_name": "来源",
  "source_url": "https://...",
  "content": "文章正文",
  "summary": "{\"thesis\":\"...\",\"facts\":[...],\"sentiment\":\"bullish\"}",
  "original_url": "https://..."
}
```

---

### 4. EntityRadar (实体雷达)

| 属性 | 说明 |
|:---|:---|
| **路由** | `/entities` |
| **API 端点** | `GET /api/entities` |
| **数据类型** | 实体列表（公司/人物/概念） |
| **缓存策略** | 无前端缓存 |

#### 响应结构

```json
[
  {
    "id": "entity-1",
    "name": "实体名称",
    "type": "company|person|concept",
    "trend": "up|down|stable",
    "mentions": 15,
    "is_subscribed": true
  }
]
```

#### 相关操作

| 操作 | 端点 | 方法 |
|:---|:---|:---|
| 订阅/取消订阅 | `/api/entities/toggle/<entity_id>` | POST |

---

### 5. DailyBriefing (每日简报)

| 属性 | 说明 |
|:---|:---|
| **路由** | `/briefing` |
| **API 端点** | `GET /api/briefing/daily` |
| **数据类型** | 每日汇总简报 |
| **缓存策略** | 无前端缓存 |

#### 响应结构

```json
{
  "date": "2025-12-29",
  "title": "Daily Intelligence Briefing",
  "subtitle": "Today's five signals you should not miss.",
  "read_time": "6 min read",
  "synthesis": "基于今日高信号情报...",
  "takeaways": ["要点1", "要点2", "要点3"],
  "top_picks": [...],
  "impact_chain": {
    "trigger": "触发事件",
    "path": ["影响路径1", "影响路径2"],
    "conclusion": "结论"
  }
}
```

---

## 🔄 数据流向图

```
┌──────────────────────────────────────────────────────────────────┐
│                         数据流向                                  │
└──────────────────────────────────────────────────────────────────┘

【原始数据流】
飞书多维表格
    ↓ Special API 同步
raw_articles 表 (source_name, source_url, title, summary, content, raw_payload)
    ↓ GET /api/raw-data
DataView 展示原始文章列表


【加工数据流】
raw_articles 表
    ↓ AI 分析 (analyze_article)
    ↓ 缓存到 ai_* 字段
raw_articles 表 (ai_polarity, ai_impacts, ai_opinion, ai_tags)
    ↓ GET /api/intelligence
IntelligenceView 展示情报卡片


【详情数据流】
raw_articles 表
    ↓ 按 ID 查询
    ↓ AI 分析（有缓存则复用）
    ↓ GET /api/article/<id>
ArticleDetail 展示文章详情
```

---

## 📊 分类体系

### 后端分类 Key

| Key | 标签 | 说明 |
|:---|:---|:---|
| `ai` | AI | 人工智能领域 |
| `digital` | 数字化 | 数字化转型 |
| `legal` | 法律法规 | 法律合规 |
| `finance` | 金融 | 金融行业 |
| `vc` | 风投 | 风险投资 |

### 前端分类定义

文件：`frontend/src/lib/data-categories.ts`

```typescript
export const DATA_CATEGORIES = [
    { id: 'ai', label: 'AI', icon: '🤖' },
    { id: 'digital', label: '数字化', icon: '💻' },
    { id: 'legal', label: '法律法规', icon: '⚖️' },
    { id: 'finance', label: '金融', icon: '💰' },
    { id: 'vc', label: '风投', icon: '🚀' }
];
```

---

## 🔐 缓存策略

### 前端 IndexedDB 缓存

| 策略 | 说明 |
|:---|:---|
| **缓存 Key** | `{接口类型}-{category}-{参数}` |
| **有效期判断** | 基于 `ingested_at` 日期，非本地时间 |
| **失效条件** | 数据日期 ≠ 今日 |
| **强制刷新** | `forceRefresh: true` 参数 |

### 后端 AI 分析缓存

| 策略 | 说明 |
|:---|:---|
| **缓存位置** | raw_articles 表 ai_* 字段 |
| **缓存 Key** | source_url |
| **有效期** | 永久（数据不变则分析结果不变） |
| **触发条件** | 首次 AI 分析时写入 |

---

## 📝 验收标准

### AC-001: 界面数据正确性
- [ ] IntelligenceView 显示 AI 分析后的情报卡片
- [ ] DataView 显示原始公众号数据
- [ ] 数据字段映射正确，无 undefined

### AC-002: 缓存有效性
- [ ] 同日数据复用缓存
- [ ] 跨日自动失效并重新获取
- [ ] 强制刷新功能正常

### AC-003: 分类筛选
- [ ] 各界面分类筛选正常
- [ ] 分类 Key 与后端一致

---

## 📅 更新日志

| 日期 | 版本 | 变更内容 |
|:---|:---|:---|
| 2025-12-29 | 1.0.0 | 初始版本，定义 5 个主要界面的数据源映射 |
